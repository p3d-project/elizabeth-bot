# Elizabeth Bot

// run on local machine
npm run register
npm run dev

// access server
ssh -i path/to/ssh/key [server-user]@[server-ip]

// Elizabeth image source (icon.png)
https://megatenwiki.com/wiki/File:P3_Elizabeth_Artwork.png

// TODO
// GHA = GitHub Actions
- Push env keys from GHA + GH (they are hard-coded right now)
- Restart server from GHA
- Automatically pull & redeploy on GHA
- Be able to change the Google Form link via Discord